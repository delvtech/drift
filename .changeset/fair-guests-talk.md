---
"@delvtech/drift": minor
---

Added missing fields to the return type of `createStub*` utils and renamed `getRandom*` utils to `random*`.
  - `getRandomAddress` is now `randomAddress`
  - `getRandomHex` is now `randomHex`
  - `getRandomInt` is now `randomInt`
